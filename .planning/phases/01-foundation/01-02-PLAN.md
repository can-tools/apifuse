---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on:
  - "01-01"
files_modified:
  - app/__init__.py
  - app/core/__init__.py
  - app/core/config.py
autonomous: true
requirements:
  - FOUND-01

must_haves:
  truths:
    - "from app.core.config import apifuse_settings succeeds with zero env vars set"
    - "apifuse_settings.app_env equals 'development' by default"
    - "apifuse_settings.is_dev returns True when app_env is 'development'"
    - "ApifuseSettings() does not raise ValidationError when unexpected env vars are present"
    - "No print() call exists in app/core/config.py"
  artifacts:
    - path: "app/__init__.py"
      provides: "app package marker"
    - path: "app/core/__init__.py"
      provides: "app.core package marker"
    - path: "app/core/config.py"
      provides: "ApifuseSettings class and apifuse_settings singleton"
      exports: ["ApifuseSettings", "apifuse_settings"]
  key_links:
    - from: "app/core/config.py"
      to: "pydantic_settings.BaseSettings"
      via: "class ApifuseSettings(BaseSettings)"
      pattern: "class ApifuseSettings"
    - from: "app/core/config.py"
      to: ".env file (optional)"
      via: "SettingsConfigDict(env_file='.env')"
      pattern: "env_file"
---

<objective>
Create the app Python package structure and the settings module that every other Phase 1 module depends on. Establishes the pydantic-settings-backed configuration singleton with zero required env vars (D-03, D-04).

Purpose: Deliver FOUND-01. The `apifuse_settings` singleton is imported by `app/core/logging.py` (Wave 2) and `main.py` (Wave 3). Package init files enable Python imports across the codebase.

Output: app/__init__.py, app/core/__init__.py, app/core/config.py with ApifuseSettings and apifuse_settings.
</objective>

<execution_context>
@C:/Workspace/cursor/apifuse/.claude/get-shit-done/workflows/execute-plan.md
@C:/Workspace/cursor/apifuse/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Workspace/cursor/apifuse/.planning/PROJECT.md
@C:/Workspace/cursor/apifuse/.planning/ROADMAP.md
@C:/Workspace/cursor/apifuse/.planning/phases/01-foundation/01-CONTEXT.md
@C:/Workspace/cursor/apifuse/.planning/phases/01-foundation/01-RESEARCH.md
@C:/Workspace/cursor/apifuse/.planning/phases/01-foundation/01-PATTERNS.md
@C:/Workspace/cursor/apifuse/.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
From .planning/phases/01-foundation/01-PATTERNS.md — config.py pattern (verbatim):
  Class: ApifuseSettings(BaseSettings)
  Singleton: apifuse_settings = ApifuseSettings()
  model_config fields: env_file=".env", env_file_encoding="utf-8", extra="ignore"
  Settings fields: app_env: str = "development", app_name: str = "apifuse", app_version: str = "0.1.0"
  Property: is_dev -> bool (returns self.app_env == "development")

Decisions honored:
  D-03: No required env vars — all fields have defaults
  D-04: BaseSettings with defaults; missing .env file silently ignored
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create app package init files</name>
  <files>app/__init__.py, app/core/__init__.py</files>
  <read_first>
    - .planning/codebase/CONVENTIONS.md (module design section — empty init files for packages)
    - .planning/codebase/ARCHITECTURE.md (package structure — app/ and app/core/ layout)
  </read_first>
  <action>
    Create app/__init__.py as an intentionally empty file containing only the comment: `# intentionally empty`.

    Create app/core/__init__.py as an intentionally empty file containing only the comment: `# intentionally empty`.

    Do NOT add any imports, re-exports, or logic to either file. These are pure package markers.
    Do NOT create any subdirectories beyond app/ and app/core/ — those are Wave 2+ concerns.
  </action>
  <verify>
    <automated>uv run python -c "import app; import app.core; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - app/__init__.py exists
    - app/core/__init__.py exists
    - Both files contain only a single comment line (`# intentionally empty`) — no imports, no code
    - `uv run python -c "import app; import app.core; print('ok')"` outputs `ok`
    - `grep -n "print(" app/__init__.py app/core/__init__.py` exits non-zero (no matches)
  </acceptance_criteria>
  <done>app/ and app/core/ are valid Python packages importable without errors</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ApifuseSettings in app/core/config.py</name>
  <files>app/core/config.py, tests/test_config.py</files>
  <read_first>
    - tests/test_config.py (fill in the stub test bodies BEFORE writing production code — TDD cycle)
    - .planning/phases/01-foundation/01-PATTERNS.md (config.py pattern section — use exactly)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 1: pydantic-settings BaseSettings with all defaults)
    - pyproject.toml (confirm pydantic-settings is in dependencies — no uv add needed)
  </read_first>
  <behavior>
    - test_settings_load_with_no_env_file: ApifuseSettings() constructs without error even if no .env file exists; assert isinstance(result, ApifuseSettings)
    - test_settings_default_app_env: ApifuseSettings().app_env == "development"
    - test_settings_default_app_name: ApifuseSettings().app_name == "apifuse"
    - test_settings_is_dev_true_in_development: ApifuseSettings().is_dev is True
    - test_settings_extra_env_vars_ignored: os.environ["SOME_UNKNOWN_VAR_XYZ"] = "1" does not raise ValidationError; cleanup env after test
  </behavior>
  <action>
    RED: Fill in the test bodies in tests/test_config.py with real assertions (replacing the xfail markers). Run `uv run pytest tests/test_config.py -x -q` — tests must FAIL because app/core/config.py does not exist.

    GREEN: Create app/core/config.py implementing ApifuseSettings exactly per the PATTERNS.md config.py pattern:
    - Import: `from pydantic_settings import BaseSettings, SettingsConfigDict`
    - Class `ApifuseSettings(BaseSettings)` with model_config using SettingsConfigDict(env_file=".env", env_file_encoding="utf-8", extra="ignore")
    - Fields: `app_env: str = "development"`, `app_name: str = "apifuse"`, `app_version: str = "0.1.0"`
    - Property `is_dev(self) -> bool` returning `self.app_env == "development"`
    - Module-level singleton: `apifuse_settings = ApifuseSettings()`

    Run `uv run pytest tests/test_config.py -x -q` — all tests must PASS.

    Naming rules (per CONVENTIONS.md and PATTERNS.md):
    - Class name is PascalCase: `ApifuseSettings` (no apifuse_ prefix on class names)
    - Singleton is module-level public identifier: `apifuse_settings` (apifuse_ prefix applies here per D-03 code_context)
    - Zero print() calls — use structlog if logging is needed (none required in config.py)
    - No unused imports
  </action>
  <verify>
    <automated>uv run pytest tests/test_config.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - app/core/config.py exists
    - app/core/config.py contains `class ApifuseSettings(BaseSettings)`
    - app/core/config.py contains `apifuse_settings = ApifuseSettings()`
    - app/core/config.py contains `extra="ignore"` in model_config
    - app/core/config.py contains `env_file=".env"` in model_config
    - `uv run python -c "from app.core.config import apifuse_settings; print(apifuse_settings.app_env)"` outputs `development`
    - `uv run python -c "from app.core.config import apifuse_settings; print(apifuse_settings.is_dev)"` outputs `True`
    - `uv run pytest tests/test_config.py -x -q` exits 0 (all 5 tests pass)
    - `grep -n "print(" app/core/config.py` exits non-zero (no matches)
  </acceptance_criteria>
  <done>ApifuseSettings loads with zero env vars; all 5 test_config tests pass; no print() calls</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| environment → ApifuseSettings | Env vars cross into settings at import time via pydantic-settings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Tampering | ApifuseSettings env var intake | mitigate | pydantic-settings type coercion validates each field against its declared Python type (str); extra="ignore" prevents unknown var injection from causing errors |
| T-01-02-02 | Information Disclosure | apifuse_settings singleton exposed at module level | accept | Settings contain no secrets in Phase 1 (all fields are public config strings); D-05 defers secret management to a later phase |
</threat_model>

<verification>
After both tasks complete:
- `uv run pytest tests/test_config.py -v` — all 5 tests pass
- `uv run python -c "from app.core.config import apifuse_settings; print(apifuse_settings.app_env)"` outputs `development`
- `uv run python -c "from app.core.config import ApifuseSettings; s = ApifuseSettings(); print(s.is_dev)"` outputs `True`
- `grep -rn "print(" app/__init__.py app/core/__init__.py app/core/config.py` exits non-zero
</verification>

<success_criteria>
- app/__init__.py and app/core/__init__.py exist as empty package markers
- app/core/config.py exposes ApifuseSettings(BaseSettings) with all defaults and apifuse_settings singleton
- All 5 test_config.py tests pass
- Zero print() calls in any app/ file created by this plan
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md` following the summary template.
</output>
