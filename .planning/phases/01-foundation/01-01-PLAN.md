---
phase: 01-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - pyproject.toml
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_config.py
  - tests/test_logging.py
  - tests/test_app.py
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03

must_haves:
  truths:
    - "uv run pytest tests/ -x -q exits 0 (all stubs collect without error)"
    - "tests/conftest.py defines a reusable async HTTP client fixture"
    - "pyproject.toml contains [tool.pytest.ini_options] with asyncio_mode = auto"
    - "No print() call exists anywhere in tests/ or main.py (static pathlib scan in test_no_print_calls_in_source — Windows-portable, no subprocess grep)"
  artifacts:
    - path: "pyproject.toml"
      provides: "pytest configuration section"
      contains: "[tool.pytest.ini_options]"
    - path: "tests/conftest.py"
      provides: "Shared async client fixture"
      exports: ["async_client"]
    - path: "tests/test_config.py"
      provides: "Config test stubs for FOUND-01"
    - path: "tests/test_logging.py"
      provides: "Logging test stubs for FOUND-02"
    - path: "tests/test_app.py"
      provides: "App integration test stubs for FOUND-03"
  key_links:
    - from: "tests/conftest.py"
      to: "main.py"
      via: "AsyncClient(app=app)"
      pattern: "from main import app"
---

<objective>
Create the test infrastructure that Wave 1+ production tasks depend on for continuous verification. This is the Nyquist-compliant Wave 0: tests exist before source code, giving every subsequent plan an immediate feedback loop.

Purpose: Enable `uv run pytest tests/ -x -q` to pass after each task commit throughout Phase 1. Tests start as stubs (xfail or skip markers) and are filled in as production code is created in Waves 1-3.

Output: pyproject.toml pytest config, tests/__init__.py, tests/conftest.py, tests/test_config.py, tests/test_logging.py, tests/test_app.py.
</objective>

<execution_context>
@./get-shit-done/workflows/execute-plan.md
@./get-shit-done/templates/summary.md
</execution_context>

<context>
@./.planning/PROJECT.md
@./.planning/ROADMAP.md
@./.planning/phases/01-foundation/01-CONTEXT.md
@./.planning/phases/01-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pytest configuration to pyproject.toml</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (read current state before modifying — append section, never replace existing content)
    - .planning/phases/01-foundation/01-VALIDATION.md (test framework requirements)
  </read_first>
  <action>
    Append a [tool.pytest.ini_options] section to pyproject.toml. The section must set:
    - asyncio_mode = "auto" (required by pytest-asyncio 1.3.0 for async test functions without per-test decorators)
    - testpaths = ["tests"] (restricts test discovery to tests/ directory)

    Do NOT remove or alter any existing [project] or [dependency-groups] sections.
    Do NOT add any other keys to the pytest section — keep it minimal.
    Use uv toolchain only; never pip.
  </action>
  <verify>
    <automated>uv run pytest --co -q 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml contains the literal text [tool.pytest.ini_options]
    - pyproject.toml contains asyncio_mode = "auto"
    - pyproject.toml contains testpaths = ["tests"]
    - `uv run python -c "import tomllib; d=tomllib.loads(open('pyproject.toml').read()); print(d['tool']['pytest.ini_options']['asyncio_mode'])"` outputs `auto`
    - Existing [project] and [dependency-groups] sections are unchanged
  </acceptance_criteria>
  <done>pyproject.toml has [tool.pytest.ini_options] with asyncio_mode="auto" and testpaths=["tests"]</done>
</task>

<task type="auto">
  <name>Task 2: Create tests package and shared conftest fixture</name>
  <files>tests/__init__.py, tests/conftest.py</files>
  <read_first>
    - pyproject.toml (verify pytest config is present before writing conftest — conftest must match asyncio_mode)
    - .planning/phases/01-foundation/01-VALIDATION.md (fixture spec: AsyncClient(app=app, base_url="http://test"))
    - main.py (understand current stub — conftest imports `app` from main; main.py is a stub now but conftest must be written for the final app object)
  </read_first>
  <action>
    Create tests/__init__.py as an empty file with a single comment line: `# tests package marker`.

    Create tests/conftest.py with:
    - Import: `import pytest` and `import pytest_asyncio` and `from httpx import AsyncClient, ASGITransport`
    - Import: `from main import app` (this import will fail until Plan 05 creates the final main.py; mark the conftest with a module-level comment explaining this)
    - A pytest fixture named `async_client` decorated with `@pytest_asyncio.fixture` (not `@pytest.fixture` — required for async fixtures in pytest-asyncio auto mode)
    - The fixture yields `AsyncClient(transport=ASGITransport(app=app), base_url="http://test")`
    - The fixture must be `async def` and use `async with AsyncClient(...) as client: yield client`
    - No other fixtures in conftest.py — single conftest rule from CLAUDE.md

    IMPORTANT: Because main.py is currently a stub with no `app` object, the conftest import will cause a collection error until Plan 05 completes. Add a `pytest.importorskip` guard or use `try/except ImportError` at module level to allow collection to proceed with a skip marker. Specifically: wrap the `from main import app` in a try/except ImportError block; set `app = None` on failure; in the fixture, use `pytest.skip("main.app not yet available")` if `app is None`.
  </action>
  <verify>
    <automated>uv run pytest tests/ --co -q 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/__init__.py exists (file present, content is a comment line only)
    - tests/conftest.py exists
    - tests/conftest.py contains `@pytest_asyncio.fixture`
    - tests/conftest.py contains `async_client`
    - tests/conftest.py contains `ASGITransport`
    - `uv run pytest tests/ --co -q` completes without import errors (may show skipped items, must not error)
    - No print() calls in tests/conftest.py: `grep -n "print(" tests/conftest.py` exits non-zero
  </acceptance_criteria>
  <done>tests/__init__.py and tests/conftest.py exist; pytest collection succeeds without errors</done>
</task>

<task type="auto">
  <name>Task 3: Create stub test files for FOUND-01, FOUND-02, FOUND-03</name>
  <files>tests/test_config.py, tests/test_logging.py, tests/test_app.py</files>
  <read_first>
    - .planning/phases/01-foundation/01-VALIDATION.md (per-requirement test map — exact test names and behaviors required)
    - .planning/phases/01-foundation/01-RESEARCH.md (section: Validation Architecture / Phase Requirements → Test Map)
    - tests/conftest.py (understand available fixtures before writing tests)
  </read_first>
  <action>
    Create three stub test files. Each file must import pytest, define all required test functions with meaningful docstrings, and mark each test body with `pytest.mark.xfail(reason="production code not yet written", strict=False)` so they are collected but not blocking.

    tests/test_config.py — covers FOUND-01. Must define:
    - `test_settings_load_with_no_env_file()`: verifies ApifuseSettings() constructs without error when no .env file is present
    - `test_settings_default_app_env()`: verifies apifuse_settings.app_env == "development" by default
    - `test_settings_default_app_name()`: verifies apifuse_settings.app_name == "apifuse"
    - `test_settings_default_app_version()`: verifies apifuse_settings.app_version == "0.1.0" by default (Plan 05 / FastAPI(version=...) dependency — 01-REVIEWS.md)
    - `test_settings_is_dev_true_in_development()`: verifies apifuse_settings.is_dev is True when app_env == "development"
    - `test_settings_extra_env_vars_ignored()`: verifies no ValidationError when unknown env vars are set

    tests/test_logging.py — covers FOUND-02. Must define:
    - `test_configure_logging_does_not_raise()`: verifies configure_logging("development") runs without exception
    - `test_configure_logging_production_uses_json_renderer()`: verifies JSONRenderer is used when app_env="production"
    - `test_no_print_calls_in_source()`: a static test using pure Python pathlib (NOT subprocess grep — Windows CI portability). Collect Path("app").rglob("*.py") plus Path("main.py"); for each file, assert "print(" does not appear in file text; on failure, list violating paths (same contract as Plan 03)

    tests/test_app.py — covers FOUND-03. Must define:
    - `test_unknown_route_returns_json_error_envelope(async_client)`: GET /nonexistent-route returns JSON body with keys "error" and "status_code"
    - `test_cors_headers_on_options_request(async_client)`: OPTIONS / with Origin header returns Access-Control-Allow-Origin header
    - `test_app_starts_and_root_returns_response(async_client)`: GET / returns any HTTP response (not a connection error)

    All test functions in test_app.py that use `async_client` must be `async def`.
    Mark ALL test function bodies with `pytest.mark.xfail(reason="...", strict=False)` at the top of each function body (not as a decorator, to allow collection even if xfail handling differs).
    Alternatively use `pytest.skip()` inside the function body — either approach is acceptable as long as `uv run pytest tests/ -x -q` collects and exits 0.

    No print() calls in any test file.
  </action>
  <verify>
    <automated>uv run pytest tests/ -x -q 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_config.py exists and contains all 6 test functions listed above (including test_settings_default_app_version)
    - tests/test_logging.py exists and contains all 3 test functions listed above
    - tests/test_app.py exists and contains all 3 test functions listed above
    - `uv run pytest tests/ -x -q` exits 0 (all tests collected; xfail/skip are acceptable outcomes)
    - `grep -rn "print(" tests/` exits non-zero (no matches)
    - `uv run pytest tests/ --co -q` lists at least 12 test items
  </acceptance_criteria>
  <done>All 12 stub tests collect and pass (xfail/skip); `uv run pytest tests/ -x -q` exits 0</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → local .py reads | test_no_print_calls_in_source scans fixed paths (app/**/*.py, main.py) via pathlib — no subprocess, no user-supplied paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Tampering | pyproject.toml pytest config | accept | Config file is version-controlled; no runtime trust boundary |
| T-01-01-02 | Information Disclosure | test_no_print_calls_in_source reads source files | accept | Test uses pathlib to read only repository .py files under app/ and main.py; no user-supplied paths |
</threat_model>

<verification>
After all three tasks complete:
- `uv run pytest tests/ -v` — all items collected, all pass (xfail/skip acceptable)
- `uv run pytest tests/ --co -q` — lists 12+ test items
- `grep -rn "print(" tests/` — exits non-zero (no matches found)
- pyproject.toml diff shows [tool.pytest.ini_options] section added, no other changes
</verification>

<success_criteria>
- pyproject.toml has [tool.pytest.ini_options] with asyncio_mode="auto" and testpaths=["tests"]
- tests/__init__.py, tests/conftest.py, tests/test_config.py, tests/test_logging.py, tests/test_app.py all exist
- `uv run pytest tests/ -x -q` exits 0
- Zero print() calls in any test file
- test_no_print_calls_in_source stub uses pathlib scanning (not subprocess grep) per Windows-portable requirement from REVIEWS.md / Plan 03 alignment
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` following the summary template.
</output>
